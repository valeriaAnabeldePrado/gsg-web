// Cliente para usar en componentes del cliente (Client Components)
export { supabase } from './client';

// Cliente admin para usar en el servidor (API Routes, Server Components)
export { supabaseAdmin } from './server';

// Helpers/SDK para operaciones comunes
export * from './helpers';
